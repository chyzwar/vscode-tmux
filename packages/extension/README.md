# VS Code Tmux

Connects each VS Code window to a persistent terminal session (tmux) shown in a companion Ghostty window. Focusing a VS Code window switches Ghostty to that workspace's terminals; `vscode file.ts:12` inside a terminal opens the file in the right window.

See the repository README for installation.
